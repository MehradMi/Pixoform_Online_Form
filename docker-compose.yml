version: "3.9"

services:
  form_backend:
    build: .
    command: python flask_backend.py
    env_file: .env
    ports:
      - "10000:10000"
    volumes:
      - ./submissions.db:/app/data/submissions.db
      - ./static:/app/static
      - ./templates:/app/templates
    restart: always